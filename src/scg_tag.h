#ifndef SCG_TAG_H
#define SCG_TAG_H


// tags for strategies and parameters
typedef enum {
        tag_I,
        tag_J,
        tag_N,
        tag_K,
        tag_SR,
        tag_NS,
        tag_HS,
        tag_LC,   // for strategy
        tag_LC_A, // for parameter id of group A.
        tag_LC_B, // for parameter id of group B
        tag_LC_T, // for parameter id of the types of A and B
} stag_t;


#endif /*SCG_TAG_H*/
